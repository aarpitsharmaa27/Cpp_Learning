#include <iostream> 
#include <cmath>        // For math functions
int main (){
      
    double x = 2;
    double y = 3;
    double z ;
    

    // z = std :: max(x, y)  ; Maximum value
    // z = std :: min(x, y) ;   Minimum Value
    // z = pow(x,y) ;           Power or x raise to y
    // z = pow (4,7);           Power or 4 raise to 7
    // z = sqrt (25);           It gives 25 is square of what
    // z = abs(324.34);         It gives absolute value i.e -324 = 324, 234.3 = 234
    z = round(234.56);       It round value to nearest number
    //z = ceil (9.01);          It round value to next number

    std :: cout << z;
   
    
    return 0;
}